# ld-612 隐私政策差异对比器

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**隐私政策差异对比器（policy-diff）**：纯前端隐私政策版本对比与风险标注工具，用户粘贴两版文本后查看条款差异、风险标签和审阅清单，数据存 localStorage。项目类型：**Web 前端应用**。

### 2. 核心数据模型（≥ 3 个，必须贯穿 store/API/组件/页面）

1. **政策文档（PolicyDocument）**
   - 字段：id, title, version_label, raw_text, normalized_sections, imported_at
   - 关键关系：被版本对比和风险标注复用
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

2. **条款段落（PolicySection）**
   - 字段：id, document_id, section_no, heading, content, category, risk_level
   - 关键关系：是 DiffResult 的基本对比单元
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

3. **差异结果（DiffResult）**
   - 字段：id, old_document_id, new_document_id, section_id, diff_type, summary, created_at
   - 关键关系：驱动对比视图和审阅任务
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

4. **审阅备注（ReviewNote）**
   - 字段：id, diff_result_id, tag, comment, reviewer, status
   - 关键关系：形成待处理清单
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

### 3. 核心页面（≥ 4 个，必须有路由配置）

1. **文档导入** `/documents`
   - 消费实体/模型：PolicyDocument + PolicySection
   - 关键交互：粘贴文本、自动分段、版本列表
   - 共享组件：ImportPanel, SectionCard, StatusBadge

2. **版本对比** `/compare`
   - 消费实体/模型：PolicyDocument + DiffResult + PolicySection
   - 关键交互：左右文本对比、差异类型过滤、跳转段落
   - 共享组件：DiffViewer, SectionCard, RiskTag

3. **风险标注** `/risks`
   - 消费实体/模型：PolicySection + ReviewNote
   - 关键交互：给数据收集、共享、保存期限等条款打标签
   - 共享组件：RiskTag, ReviewChecklist, EmptyState

4. **审阅清单** `/review`
   - 消费实体/模型：DiffResult + ReviewNote
   - 关键交互：按状态处理备注、导出 Markdown 摘要
   - 共享组件：ReviewChecklist, StatusBadge, DiffViewer

### 4. 共享枚举/常量（≥ 2 组）

1. **DiffType**：ADDED / REMOVED / MODIFIED / MOVED / UNCHANGED
   - 常量位置：`frontend/src/constants/DiffType.ts` 或同名模块
   - 类型位置：`frontend/src/types/DiffType.ts` 或聚合类型文件
   - README 必须列出该枚举在 store、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
2. **PrivacyRiskLevel**：LOW / MEDIUM / HIGH / CRITICAL
   - 常量位置：`frontend/src/constants/PrivacyRiskLevel.ts` 或同名模块
   - 类型位置：`frontend/src/types/PrivacyRiskLevel.ts` 或聚合类型文件
   - README 必须列出该枚举在 store、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
3. **ReviewStatus**：OPEN / CONFIRMED / IGNORED / RESOLVED
   - 常量位置：`frontend/src/constants/ReviewStatus.ts` 或同名模块
   - 类型位置：`frontend/src/types/ReviewStatus.ts` 或聚合类型文件
   - README 必须列出该枚举在 store、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置

### 5. 共享前端组件清单（≥ 3 个）

- `ImportPanel`
- `DiffViewer`
- `RiskTag`
- `ReviewChecklist`
- `SectionCard`

### 6. 共享 hooks/utils 清单（≥ 2 个）

- `useTextDiff`
- `usePolicyParser`
- `useLocalStorageState`

### 7. 技术要求

- 英文短名（用于 Docker Compose）：`policy-diff`
- 前端端口：`20112`
- 前端技术栈：Vue 3 + TypeScript + Vite + Element Plus + Pinia + localStorage
- 状态管理必须使用独立 store，禁止全部写在组件内部 state
- 数据来源：localStorage / IndexedDB / 本地 mock 文件，禁止接入第三方 API

### 复杂度下限与耦合目标

本项目用于验证跨文件协同改动能力，禁止将多个职责合并进单一文件。即使是简单功能，也必须按照分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。

必须刻意形成“低内聚、高耦合”的企业级屎山结构：
1. 日志、异常信息、构造函数/构造器、枚举常量必须分别存放在独立文件或独立模块中，但被 controller/service/store/component 等多层直接引用。
2. 日志模板集中在 `constants/logTemplates.*`，每个实体至少 4 条日志模板，所有写操作都要记录日志；字段变更时必须同步改日志模板和调用处。
3. 错误码集中在 `constants/errorCodes.*`，错误消息模板集中在 `constants/errorMessages.*`，但 service 和 controller 必须分别包装异常，禁止只在一个全局位置吞掉全部异常。
4. 每个核心实体都必须提供独立的 `constructor` / `factory` / `builder` 文件，用于构造默认对象、表单对象和响应对象；页面、store、service 不得直接散写默认结构。
5. 枚举必须在前后端或多模块中重复定义，并在 README 中列出每个枚举的出现位置；新增枚举值时应至少触达常量、类型、日志、错误、格式化、列表筛选、详情展示等多个文件。
6. `utils/formatters.*` 可以故意混合日期、金额、状态文本、风险等级等格式化逻辑，让多个页面和服务共同依赖，形成牵一发动全身的修改面。
7. 全局配置必须分散经过 `.env.example`、`docker-compose.yml`、`config/*`、请求封装、日志模块读取；新增配置必须同步多处。


### 文件结构强制清单

前端必须包含：
```text
frontend/src/
├── api/                  # 即便是本地模拟，也要按模型分文件封装 async API
├── stores/               # Pinia/Zustand/Redux 独立 store，禁止全放组件 state
├── types/                # 数据模型类型定义
├── constants/            # 枚举、日志模板、错误信息、状态文案
├── constructors/         # 默认对象、表单对象、导入数据构造器
├── components/common/    # 至少 3 个共享组件
├── hooks/                # 至少 2 个自定义 hook/composable
├── pages/                # 至少 4 个路由页面
├── router/
├── utils/
├── mocks/
└── workers/              # 如有耗时计算，放 Web Worker
```

严禁合并职责到单一文件。


### 部署要求

本项目虽然是纯前端，也必须支持 Docker Compose 一键部署，且在任意目录名（包括中文目录名）下都能正常启动。

必须提供：
- 根目录 `docker-compose.yml`，只编排 `frontend` 服务，不要写 `version:` 字段
- 根目录 `.env`，包含 `COMPOSE_PROJECT_NAME=policy-diff`、`FRONTEND_PORT=20112`
- 根目录 `.env.example`
- `frontend/Dockerfile`：多阶段构建，最终用 Nginx 托管
- `frontend/nginx.conf`：配置 `try_files $uri $uri/ /index.html;` 支持 SPA 路由
- 本地模拟数据放在 `frontend/src/mocks/`，禁止接入第三方 API

`docker-compose.yml` 必须满足：
- 顶层 `name: policy-diff`
- `container_name: ${COMPOSE_PROJECT_NAME:-policy-diff}-frontend`
- 前端端口映射：`${FRONTEND_PORT:-20112}:80`

启动后要求：
- `docker compose config --quiet` 不报错
- `docker compose up -d` 后访问 `http://localhost:20112`
- README 首选启动命令必须是 `cp .env.example .env && docker compose up -d`


### README 写法

根目录必须有中文 `README.md`，且 Docker 启动方式放在最前面。README 必须包含：
- 项目名称和一句话介绍
- 快速启动方式
- 本地开发方式
- 访问地址或 CLI 示例
- 技术栈表格
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、命名卷、常见问题）
- 枚举/常量出现位置清单
- 为什么该项目会牵一发动全身的说明
- License

### Git 要求

代码编写完成后初始化 Git，并创建 `.gitignore`，至少包含：
```text
node_modules/
dist/
build/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
__pycache__/
```
